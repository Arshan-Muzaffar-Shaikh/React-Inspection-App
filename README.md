# React-Inspection-App
The application was built using React.js for the frontend interface and Firebase for backend services such as Authentication and Firestore Database. The project also uses Capacitor to convert the React web application into an Android APK for mobile usage. PDF inspection reports are generated using jsPDF
