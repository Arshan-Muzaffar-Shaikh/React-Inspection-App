import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aepl.inspection',
  appName: 'AEPL Electrical Inspection',
  webDir: 'build'
};

export default config;
